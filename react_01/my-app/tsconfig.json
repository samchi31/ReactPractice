{
    "compilerOptions": {
        "outDir": "./dist/",      // //js파일 아웃풋 경로바꾸기
        "sourceMap": true,
        //"noImplicitAny": true,  // any 타입이 의도치 않게 발생할 경우 에러를 띄워줌
        "module": "ES6",        // 파일 간 import 문법을 구현할 때 어떤 문법을 쓸지
        "target": "ES5",        // 타입스크립트 파일을 어떤 버전의 자바스크립트로 변경할지
        "jsx": "react",       // tsx 파일을 jsx로 어떻게 컴파일할 것인지
        "allowJs": true,      // js 파일들 ts에서 import해서 쓸 수 있는지
        //"moduleResolution": "node",
        "allowSyntheticDefaultImports": true,  // import 구문 사용 시 as 별칭 생략 하고 바로 사용 가능 여부
        "types": ["react/next", "react-dom/next"],
    }
}